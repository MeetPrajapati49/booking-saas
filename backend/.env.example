# This MVP round runs self-contained (SQLite, no external services) so it's
# guaranteed to run for the reviewer without any account setup. Nothing here
# is required to start the app locally.

PORT=4000
JWT_SECRET=dev-secret-change-me

# --- Not used in this round, listed for the production mapping below ---
# SUPABASE_URL=
# SUPABASE_ANON_KEY=
# SUPABASE_SERVICE_ROLE_KEY=
# STRIPE_SECRET_KEY=
# STRIPE_WEBHOOK_SECRET=
